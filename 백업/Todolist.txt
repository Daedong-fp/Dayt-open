Todolist

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MHD To Do List</title>
    <link rel="stylesheet" href="Dayt_todolist.css">
  </head>
  <element class="nanum-myeongjo-regular"></element>
  <body>
    <header>

      <div class="logo">
        <img src="/images/4-removebg-preview.png" alt="" width="50px" height="50px">
      </div>

      <div class="nav_field">
        <div class="nav">
          <a href="DaytCalander.html" target="_blank" id="a">MAIN</a>
          <a href="DaytCalander.html" target="_blank" id="a">WORKSPACE</a>
          <a href="DaytCalander.html" target="_blank" id="b">DAILY</a>
          <a href="DaytCalander.html" target="_blank" id="c">CALENDER</a>
        </div>
      </div>

      <div class="logoImg">
        <p>User</p>
        <p>님</p>
        <img src="/images/Vector-1.png" width="20px" height="25px">
        <img src="/images/Group 46.png" width="25px">
      </div>

    </header>

    <div class="today">
      <h1>2008.10.11.MON</h1>
    </div>
    
    <div class="todocontainer">
      <div class="todo">
        <p>ToDo</p>
      </div>
  
      <div class="done">
        <p>Done</p>
      </div>
    </div>


      <script src="Dayt.todolist.js"></script>
  </body>
</html>

CSS

html{
  height: 100vh;
}

@font-face {
  font-family: 'Pretendard-Regular';
  src: url('https://fastly.jsdelivr.net/gh/Project-Noonnu/noonfonts_2107@1.1/Pretendard-Regular.woff') format('woff');
  font-weight: 500;
  font-style: normal;
}

body{
  margin: 0px;
  font-family: 'Pretendard-Regular';
  font-weight: 700;
  font-style: normal;
  background-color: white;
  text-align: center;
}

header{
  background-color: #ffffff;
  height: 70px;
  display: flex; 
  align-items: center;
  justify-content: center; 
  justify-content: space-between;
  border-bottom: 1px solid rgba(0, 0, 0, 0.295);
}

.logo {
  display: flex;
  align-items: center;
  padding-left: 35px;
  padding-right: 55px;
}

.logo h1, .logo p {
  margin: 0px;
  color: white;
}

.logo h1 {
  font-size: 24px;
  padding: 10px;
}

.logo p {
  font-size: 15px;
  font-weight: 700;
}

.logoImg {
  display: flex;
}

.logoImg img{
  margin: auto;
  padding: auto;
  display: flex;
  margin-left: 20px;
  margin-right: 15px;
}

.logoImg img:first-child{
  height: 25px;
  width: 20px;
}

.logoImg img:last-child{
  margin-right: 100px;
}

.logoImg p{
  font-size: 15px;
  display: flex;
  align-items: center;
}

.logoImg p:nth-child(2){
  font-size: 15px;
  display: flex;
  align-items: center;
  padding-left: 5px;
}

.nav{
  height: 70px;
  display: flex;
  gap: 55px;
  justify-content: flex-start;
  text-align: center;
  align-items: center;
  padding: 0;
}

.nav_field {
  display: flex;
  justify-content: start;
  align-items: center;
  width: 80%;
  height: 70px;
  padding-right: 120px;
}

.nav a{
  color: black;
  text-decoration: none;
}

.today{
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 15px;
  position: relative;
  top: 80px;

}

.todocontainer{
  display: flex;
  justify-content: center;
  margin-top: 105px;
  background-color: white;
}

.todo{
  display: flex;
  background-color: rgb(255, 255, 255);
  justify-content: center;
  width: 440px;
  height: 540px;
  margin: 20px;
  margin-right: 45px;
  border: 1px solid rgba(0, 0, 0, 0.3);
  border-radius: 20px;
}

.todo p{
  border-bottom: 1px solid rgba(0, 0, 0, 0.3);
  margin: 0px;
  padding: 0px;
  padding-top: 8px;
  padding-bottom: 5px;
  height: 50px;
  width: 440px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 30px;
}

.done{
  display: flex;
  background-color: rgb(255, 255, 255);
  justify-content: center;
  width: 440px;
  height: 540px;
  margin: 20px;
  margin-left: 45px;
  border: 1px solid rgba(0, 0, 0, 0.3);
  border-radius: 20px;
}

.done p{
  border-bottom: 1px solid rgba(0, 0, 0, 0.3);
  margin: 0px;
  padding: 0px;
  padding-top: 8px;
  padding-bottom: 5px;
  height: 50px;
  width: 440px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 30px;
}

Js

document.addEventListener('DOMContentLoaded', init) 

function init() {
  const taskInput = document.querySelector('#willTodo')
  const addTaskButton = document.querySelector('#todoButton')
  const taskList = document.querySelector('#list_menu')

  addTaskButton.addEventListener('click', addTask)
  taskInput.addEventListener('keydown', function(event) {
    if(event.key === 'Enter') {
      addTask()
    }
  })

  function addTask() {
    const taskText = taskInput.value.trim()

    if(taskText !== '') {
      const li = document.createElement('li')
      li.className = 'task_Item'

      const now = new Date()
      const month = now.getMonth() + 1
      const day = now.getDate()

      ///리스트를 올린 날짜
      const dateSpan = document.createElement('span')
      dateSpan.className = 'task_Date'
      dateSpan.textContent = dateText


      ///입력한 리스트
      const taskSpan = document.createElement('span')
      taskSpan.className = 'task_Text'
      taskSpan.textContent = taskText


      ///삭제 버튼
      const delButton = document.createElement('button')
      delButton.className = 'del_button'
      delButton.textContent = 'del'
      delButton.addEventListener('click', function() {
        taskList.removeChild(li)
      })


      ///수정 버튼
      const editButton = document.createElement('button')
      editButton.className = 'edit_button'
      editButton.textContent = 'edit'
      editButton.addEventListener('click', function() {
        const newTaskText = prompt('새로운 할 일을 입력하세요:', taskText)
        if(newTaskText !== null && newTaskText.trim() !== '') {
          taskSpan.textContent = newTaskText.trim()
        }
      })

      li.appendChild(dateSpan)
      li.appendChild(taskSpan)
      li.appendChild(editButton)
      li.appendChild(delButton)
      taskList.appendChild(li)
      taskInput.value = ''
    }
  }
}